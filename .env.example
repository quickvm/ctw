# Copy to .env in any project directory, or export as shell env vars.
# For named profiles, see ~/.config/ctw/config.toml

# Default tracker (profile name from config.toml, or provider name)
CTW_DEFAULT_TRACKER=work

# Linear
CTW_LINEAR_API_KEY=lin_api_your_key_here
CTW_LINEAR_TEAM_ID=your_team_id_here

# GitHub
CTW_GITHUB_TOKEN=ghp_your_token_here
CTW_GITHUB_REPO=owner/repo

# GitHub auth method: "token" (default) or "gh-cli"
# CTW_GITHUB_AUTH=gh-cli

# Per-project provider override (bypasses default_tracker)
# CTW_PROVIDER=github
